package main

// Version is the single source of truth for the application version.
// Released versions start at 0.5.0 (the first public release) and go up from
// there. Keep this in sync with wails.json's "info.productVersion".
const Version = "0.5.1"
