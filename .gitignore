build/bin
node_modules
frontend/dist
frontend/test-results/
frontend/playwright-report/

# Local dev / personal — not part of the published project
.taskmaster/
.playwright-mcp/
CLAUDE.md
TASKMASTER_INTEGRATION_PLAN.md
energott.html

# User-specific dictation config (contains personal API key / usage)
dictation/settings.json
dictation/api_usage_log.json
*.deb
*.rpm

# Test binary that `go test` leaves in the package root; the real build
# output goes to build/bin.
/asmgr-desktop

# IDE settings — per-developer, not per-project.
.idea/

# Helyi aláírási segédszkript: személyes adatokat tartalmaz (Apple ID, Team ID,
# a .p12 jelszavának helye), a repó pedig publikus.
scripts/setup-macos-signing-secrets.sh
